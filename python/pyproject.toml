[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ccxt"
version = "4.5.4"
description = "Custom CCXT with Wallex"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
include = ["ccxt*"]
